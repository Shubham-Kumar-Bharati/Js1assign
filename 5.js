// Question No.5:-You are provided a four digit number N only. Your task is to print out the last digit of that number.

const Last_Digit = (n) => {
    let result=n%10;
    return(result);
 }
 console.log(Last_Digit(4567));
 